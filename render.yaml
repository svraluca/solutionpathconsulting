services:
  - type: web
    name: solutionpath-consulting
    env: node
    buildCommand: npm install
    startCommand: npm run prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: EMAIL_PASSWORD
        sync: false 